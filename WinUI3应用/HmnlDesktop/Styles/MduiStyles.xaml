<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HmnlDesktop">
    
    <!-- MDUI 圆角系统 -->
    <x:Double x:Key="CornerRadiusMduiNone">0</x:Double>
    <x:Double x:Key="CornerRadiusMduiSm">8</x:Double>
    <x:Double x:Key="CornerRadiusMduiMd">12</x:Double>
    <x:Double x:Key="CornerRadiusMduiLg">16</x:Double>
    <x:Double x:Key="CornerRadiusMduiXl">20</x:Double>
    <x:Double x:Key="CornerRadiusMdui2Xl">24</x:Double>
    <x:Double x:Key="CornerRadiusMduiFull">9999</x:Double>
    
    <!-- 圆角资源 -->
    <CornerRadius x:Key="CornerRadiusMduiNoneValue" TopLeft="0" TopRight="0" BottomLeft="0" BottomRight="0" />
    <CornerRadius x:Key="CornerRadiusMduiSmValue" TopLeft="8" TopRight="8" BottomLeft="8" BottomRight="8" />
    <CornerRadius x:Key="CornerRadiusMduiMdValue" TopLeft="12" TopRight="12" BottomLeft="12" BottomRight="12" />
    <CornerRadius x:Key="CornerRadiusMduiLgValue" TopLeft="16" TopRight="16" BottomLeft="16" BottomRight="16" />
    <CornerRadius x:Key="CornerRadiusMduiXlValue" TopLeft="20" TopRight="20" BottomLeft="20" BottomRight="20" />
    <CornerRadius x:Key="CornerRadiusMdui2XlValue" TopLeft="24" TopRight="24" BottomLeft="24" BottomRight="24" />
    <CornerRadius x:Key="CornerRadiusMduiFullValue" TopLeft="9999" TopRight="9999" BottomLeft="9999" BottomRight="9999" />
    
    <!-- 控件圆角 -->
    <CornerRadius x:Key="ButtonCornerRadius">12</CornerRadius>
    <CornerRadius x:Key="CardCornerRadius">16</CornerRadius>
    <CornerRadius x:Key="TextBoxCornerRadius">12</CornerRadius>
    <CornerRadius x:Key="ComboBoxCornerRadius">12</CornerRadius>
    <CornerRadius x:Key="PasswordBoxCornerRadius">12</CornerRadius>
    <CornerRadius x:Key="ImageCornerRadius">12</CornerRadius>
    <CornerRadius x:Key="AvatarCornerRadius">9999</CornerRadius>
    <CornerRadius x:Key="DialogCornerRadius">16</CornerRadius>
    <CornerRadius x:Key="FlyoutCornerRadius">12</CornerRadius>
    <CornerRadius x:Key="ToolTipCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="ProgressBarCornerRadius">8</CornerRadius>
    
    <!-- 阴影系统 -->
    <Thickness x:Key="ShadowSmallOffset">0, 2, 0, 4</Thickness>
    <Thickness x:Key="ShadowMediumOffset">0, 4, 0, 8</Thickness>
    <Thickness x:Key="ShadowLargeOffset">0, 8, 0, 16</Thickness>
    
    <Color x:Key="ShadowColor">#000000</Color>
    
    <!-- 阴影效果 -->
    <DropShadowEffect x:Key="ShadowSmall"
                      BlurRadius="8"
                      Color="{StaticResource ShadowColor}"
                      Opacity="0.1"
                      Direction="270"
                      ShadowDepth="2" />
    
    <DropShadowEffect x:Key="ShadowMedium"
                      BlurRadius="12"
                      Color="{StaticResource ShadowColor}"
                      Opacity="0.15"
                      Direction="270"
                      ShadowDepth="4" />
    
    <DropShadowEffect x:Key="ShadowLarge"
                      BlurRadius="24"
                      Color="{StaticResource ShadowColor}"
                      Opacity="0.2"
                      Direction="270"
                      ShadowDepth="8" />
    
    <!-- 卡片样式 -->
    <Style x:Key="CardStyle" TargetType="Grid">
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusMduiLgValue}" />
        <Setter Property="Background" Value="{ThemeResource CardBackground}" />
        <Setter Property="BorderBrush" Value="{ThemeResource CardBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Effect" Value="{StaticResource ShadowMedium}" />
        <Setter Property="Padding" Value="16" />
        <Setter Property="Margin" Value="8" />
    </Style>
    
    <!-- 按钮样式 -->
    <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
        <Setter Property="CornerRadius" Value="{StaticResource ButtonCornerRadius}" />
        <Setter Property="Background" Value="{ThemeResource PrimaryButtonBackground}" />
        <Setter Property="Foreground" Value="{ThemeResource PrimaryButtonForeground}" />
        <Setter Property="Effect" Value="{StaticResource ShadowSmall}" />
    </Style>
    
    <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
        <Setter Property="CornerRadius" Value="{StaticResource ButtonCornerRadius}" />
        <Setter Property="Background" Value="{ThemeResource SecondaryButtonBackground}" />
        <Setter Property="Foreground" Value="{ThemeResource SecondaryButtonForeground}" />
    </Style>
    
    <Style x:Key="OutlineButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
        <Setter Property="CornerRadius" Value="{StaticResource ButtonCornerRadius}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{ThemeResource OutlineButtonBorderBrush}" />
        <Setter Property="Foreground" Value="{ThemeResource OutlineButtonForeground}" />
    </Style>
    
    <!-- 输入框样式 -->
    <Style x:Key="TextBoxStyle" TargetType="TextBox">
        <Setter Property="CornerRadius" Value="{StaticResource TextBoxCornerRadius}" />
        <Setter Property="Background" Value="{ThemeResource TextBoxBackground}" />
        <Setter Property="BorderBrush" Value="{ThemeResource TextBoxBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="Foreground" Value="{ThemeResource TextBoxForeground}" />
        <Setter Property="FontSize" Value="14" />
    </Style>
    
    <Style x:Key="PasswordBoxStyle" TargetType="PasswordBox">
        <Setter Property="CornerRadius" Value="{StaticResource PasswordBoxCornerRadius}" />
        <Setter Property="Background" Value="{ThemeResource TextBoxBackground}" />
        <Setter Property="BorderBrush" Value="{ThemeResource TextBoxBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="Foreground" Value="{ThemeResource TextBoxForeground}" />
        <Setter Property="FontSize" Value="14" />
    </Style>
    
    <!-- 组合框样式 -->
    <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
        <Setter Property="CornerRadius" Value="{StaticResource ComboBoxCornerRadius}" />
        <Setter Property="Background" Value="{ThemeResource ComboBoxBackground}" />
        <Setter Property="BorderBrush" Value="{ThemeResource ComboBoxBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="Foreground" Value="{ThemeResource ComboBoxForeground}" />
        <Setter Property="FontSize" Value="14" />
    </Style>
    
    <!-- 列表项样式 -->
    <Style x:Key="ListViewItemStyle" TargetType="ListViewItem">
        <Setter Property="Padding" Value="16" />
        <Setter Property="Margin" Value="0, 4, 0, 4" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <Grid CornerRadius="{StaticResource CornerRadiusMduiMdValue}" 
                          Background="{TemplateBinding Background}" 
                          BorderBrush="{TemplateBinding BorderBrush}" 
                          BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter Content="{TemplateBinding Content}" 
                                          ContentTemplate="{TemplateBinding ContentTemplate}" 
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                          Padding="{TemplateBinding Padding}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- 滚动查看器样式 -->
    <Style x:Key="ScrollViewerStyle" TargetType="ScrollViewer">
        <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="VerticalScrollMode" Value="Auto" />
        <Setter Property="HorizontalScrollMode" Value="Auto" />
        <Setter Property="IsHorizontalRailEnabled" Value="True" />
        <Setter Property="IsVerticalRailEnabled" Value="True" />
        <Setter Property="ZoomMode" Value="Disabled" />
        <Setter Property="IsDeferredScrollingEnabled" Value="False" />
        <Setter Property="BringIntoViewOnFocusChange" Value="True" />
    </Style>
    
    <!-- 进度条样式 -->
    <Style x:Key="ProgressBarStyle" TargetType="ProgressBar">
        <Setter Property="Height" Value="8" />
        <Setter Property="CornerRadius" Value="{StaticResource ProgressBarCornerRadius}" />
        <Setter Property="Background" Value="{ThemeResource ProgressBarBackground}" />
        <Setter Property="Foreground" Value="{ThemeResource ProgressBarForeground}" />
    </Style>
    
</ResourceDictionary>
